// Homebrew.js 
// Custom Brew for MEAN Stack
// Rohan Krishna
$(document).ready(function() {
	var sliderWrapper = $('#sliderWrapper');
	$(sliderWrapper).owlCarousel({
		loop: false,
		nav: false,
		center: true,
		items: 1,
	});
});